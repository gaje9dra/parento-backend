# Application
NODE_ENV=development
HOST=127.0.0.1
PORT=3000
LOG_LEVEL=info

# Database (not used by Phase 1.1; required by later database phases)
DATABASE_URL=

# Realtime (boundary only in Phase 1.1)
REALTIME_ENABLED=false
